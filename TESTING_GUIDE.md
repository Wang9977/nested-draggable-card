# 完整测试指南

## 快速开始

### 环境设置
1. 项目运行中: http://localhost:5173
2. 打开浏览器DevTools查看console输出
3. 确保没有JavaScript错误

### 添加卡片
- 点击"添加卡片"按钮添加新卡片
- 每次点击会创建一张新卡片，名称为"卡片名称N"

### 转换为条件组
- 悬停在卡片上
- 点击"扩展"按钮（仅在卡片未转换时显示）
- 卡片将变成"条件组"，原卡片成为其唯一的子项

## 核心测试场景

### 场景1: 卡片在根level重新排序（基础功能）
**目的**: 验证基本的拖拽排序

**步骤**:
1. 添加3张卡片: Card1, Card2, Card3
2. 拖动 Card1 到 Card3 上方
3. 释放

**预期结果**:
- Card1 移动到 Card3 上方
- 最终顺序: Card2, Card1, Card3
- 无console错误

**验证**:
- ✅ 卡片顺序改变
- ✅ 拖拽过程显示红线
- ✅ 其他卡片不受影响

---

### 场景2: 卡片和条件组交换位置 【修复内容1】
**目的**: 验证卡片-组交换的修复

**步骤**:
1. 添加2张卡片: Card1, Card2
2. 将 Card2 转换为 Group2
3. 拖动 Card1 **上方悬停** Group2 并释放

**预期结果**:
- Card1 和 Group2 交换位置
- 最终顺序: Group2, Card1
- Group2 内的卡片不变

**验证**:
- ✅ 视觉反馈正确（红线在Group2上方）
- ✅ 释放后位置改变
- ✅ Group内容保留

**如果失败**:
- 检查 useCardDrag.ts 第108行: `&& !isUpDragValue` 条件
- 检查 console 输出 [DROP HANDLER] 日志

---

### 场景3: 从条件组中提取卡片 【修复内容2】
**目的**: 验证卡片可以从组中拖出

**步骤**:
1. 添加3张卡片: Card1, Card2, Card3
2. 将 Card2 转换为 Group2（此时 Group2 内有 Card2）
3. 从 Group2 展开查看内部卡片
4. 拖动 Group2 内的卡片 **上方悬停** Card3 并释放

**预期结果**:
- 卡片出现在 Card3 上方
- Group2 仍存在但可能为空
- 最终顺序: Card1, Group2, 提取的卡片, Card3

**验证**:
- ✅ 拖拽过程显示红线
- ✅ 释放后卡片移出组
- ✅ 卡片不会消失

**如果失败**:
- 检查 Wrapper.tsx 第64-106行的提取逻辑
- 确认没有使用 deleteCard

---

### 场景4: 单卡片条件组提取 【修复内容3 - 关键】
**目的**: 验证单卡片组提取时不会导致卡片消失

**步骤**:
1. 添加1张卡片: Card1
2. 点击"扩展"将 Card1 转换为 Group1
   - 现在只有 Group1 (内部包含 Card1)
3. 从 Group1 内拖出 Card1 到 Group1 上方并释放

**预期结果**:
- ✅ Card1 出现在 Group1 上方
- ✅ Card1 **没有消失**
- 最终: Card1, Group1(空)

**关键验证点**:
- 在步骤3释放时，Card1应该仍然可见
- 如果Card1消失，说明修复3未生效

**如果卡片消失**:
- 这是修复前的bug症状
- 原因: deleteCard函数删除了空组
- 解决: 检查 Wrapper.tsx 是否正确手动处理children删除

---

### 场景5: 卡片合并进条件组 【可选功能】
**目的**: 验证可以将卡片合并进组

**步骤**:
1. 添加3张卡片: Card1, Card2, Card3
2. 将 Card2 转换为 Group2
3. 拖动 Card1 **下方悬停** Group2 并释放

**预期结果**:
- Card1 被添加到 Group2 的children
- 打开 Group2 可看到 Card1
- Card1 从根level消失

**验证**:
- ✅ 视觉反馈正确（红线在Group2下方）
- ✅ 释放后 Card1 进入 Group2
- ✅ Group2 内现在有多个卡片

---

## 完整工作流测试

### 完整场景: 复杂的拖拽操作序列
**目的**: 综合测试所有修复

**步骤**:
1. 添加5张卡片: C1, C2, C3, C4, C5
2. C2转换为Group A, C4转换为Group B
3. 将C1拖到Group A上方 → 交换
4. 将Group A内的卡片拖到C5上方 → 提取
5. 将C3拖到Group B下方 → 合并

**预期**:
- 所有操作正确执行
- 无卡片消失
- 无console错误
- 最终结构正确

---

## 调试技巧

### 启用详细日志
在浏览器console中查看:
- `[DROP HANDLER]` - 记录drop事件的决策逻辑
- `[MOVE_CARD_*]` - 记录moveCard的执行分支
- `[EXTRACT_FROM_GROUP]` - 记录卡片提取过程 (已禁用，需启用见调试)

### 检查最终状态
```javascript
// 在console中查看最终的卡片结构
// (需要在React DevTools中查看state)
```

### 常见问题排查

| 症状 | 可能原因 | 检查位置 |
|------|---------|---------|
| 卡片-组交换不动 | Drop handler路由错误 | useCardDrag.ts:108 |
| 卡片无法拖出组 | Drop handler未识别 | useCardDrag.ts:98-126 |
| 卡片拖出后消失 | deleteCard删除了空组 | Wrapper.tsx:64-106 |
| 无视觉反馈 | ref未挂载到DOM | Card.tsx:154-157 |
| Group未展开 | Container未渲染children | Card.tsx:141-149 |

---

## 性能测试 (可选)

### 大量卡片性能
1. 添加100+张卡片
2. 进行拖拽操作
3. 检查帧率是否保持流畅 (目标>30fps)

### 深层嵌套 (如果支持)
1. 创建嵌套的groups
2. 进行跨层拖拽
3. 验证所有操作都能正确执行

---

## 成功标准

所有以下条件都满足才算修复成功:

- ✅ 场景2: 卡片-组交换正常工作
- ✅ 场景3: 卡片能从组中提取
- ✅ 场景4: 单卡片组提取时卡片不消失
- ✅ 场景5: 卡片能合并进组(可选)
- ✅ 完整场景: 复杂操作序列正确执行
- ✅ 无JavaScript errors
- ✅ 无内存泄漏

---

## 回归测试清单

在任何代码变更后，运行以下清单:

- [ ] 基础排序功能
- [ ] 卡片-组交换
- [ ] 卡片提取
- [ ] 单卡片组提取 (最关键)
- [ ] 卡片合并
- [ ] 删除功能
- [ ] 操作符切换
- [ ] Console无错误

